{
  "buildCommand": "mkdir -p public && cp -R studio/. public/",
  "rewrites": [
    { "source": "/", "destination": "/index.html" },
    { "source": "/canvas.html", "destination": "/canvas.html" }
  ]
}
